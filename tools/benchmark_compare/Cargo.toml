[package]
name = "bench_askama"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
askama = "0.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = true
codegen-units = 1
